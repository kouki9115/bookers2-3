<main>
 <p id="notice"></p>
  <div class="container">
   <div class="row">
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">

<h1>Editing Book</h1>
<%= form_with model:@book, local:true do |f| %>
  <div><label for="inputName">Title</label><br>
  <%= f.text_field :title, autofocus: true, id:"inputTitle", placeholder:"Title"%></div>

  <div><label for="inputOpinion">Opinion</label><br>
  <%= f.text_area :body, autofocus: true, id:"inputopinion" %></div>

  <%= f.submit "Update Book",class: "btn btn-success"  %>
<% end %>
   <%= link_to "Show", book_path(@book) %>
   <%= link_to "Back", books_path %>
   </div>
  </div>
 </div>
</main>
